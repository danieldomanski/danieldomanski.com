import styled from 'styled-components'
import tw from 'tailwind.macro'

export default styled.div`
  ${tw`inline-flex flex-col justify-center items-start w-full h-full absolute`};
  top: 50%;
  transform: translateY(-50%);
`
