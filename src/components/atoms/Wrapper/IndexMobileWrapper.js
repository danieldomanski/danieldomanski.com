import styled from 'styled-components'
import tw from 'tailwind.macro'

export default styled.div`
  ${tw`flex flex-col w-full h-full relative bg-primary-100`};
  z-index: 5;
  padding-top: 100px;
  padding-bottom: 600px;
`
